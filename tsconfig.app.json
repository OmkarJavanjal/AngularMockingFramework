/* To learn more about this file see: https://angular.io/config/tsconfig. */
{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "outDir": "./out-tsc/app",
    "types": []
  },
  "files": [
    "src/main.ts",
    //"src/maintenance.ts", //maintenancne.ts can be added here also but get warning in console, so to avoid it tsconfigmaintenance.app is added
    "src/polyfills.ts"
  ],
  "include": [
    "src/**/*.d.ts"
  ]
}
